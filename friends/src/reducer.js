// import action types
// import {
// } from './actions'

// state initial values
const initialState = {
    friends: [],
}

export default function(state = initialState, action) {
	switch (action.type) {
        //  insert reducer cases here
        default:
            return state
    }
}